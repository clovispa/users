const getters = {
    userListState: state => state.userState,
    userTodoState: state => state.userTodo,
    addUserState: state => state.adduser,
    accessUserState: state => state.userValidate,

}
export default getters;
